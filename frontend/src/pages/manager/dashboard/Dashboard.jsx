import React from "react";
import { useNavigate } from "react-router-dom";
const Dashboard = () => {
  const navigation = useNavigate();

  return <>
    <h1 className="text-red-500">heda</h1>
  </>;
};

export default Dashboard;
